# Map ambient audio

**`audio.mp3`** — referenced in the app as `/audio/audio.mp3`.

Looping ambience for the map page only (~18% volume, fade in/out). Does not autoplay; starts after the user interacts with the map or toggles atmosphere on.
